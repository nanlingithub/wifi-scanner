╔════════════════════════════════════════════════════════════════════╗
║          WiFi专业工具 - 测试快速参考卡片                            ║
╚════════════════════════════════════════════════════════════════════╝

📋 基础命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  python run_tests.py              # 运行所有测试（推荐）
  python run_tests.py --quick      # 快速测试（跳过慢速）
  python run_tests.py --summary    # 显示测试摘要
  运行测试.bat                      # Windows批处理脚本


🎯 选择性测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  --marker integration             # 集成测试
  --marker performance             # 性能测试
  --marker slow                    # 慢速测试
  --marker admin_required          # 需要管理员权限
  --file test_wifi6_analyzer.py    # 特定文件
  --failed                         # 重新运行失败的测试


📊 报告生成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  --ci                             # CI模式（完整报告）
  --coverage-only                  # 仅覆盖率报告
  --no-coverage                    # 不生成覆盖率
  --no-html                        # 不生成HTML报告


📁 报告位置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  test_reports/report_*.html       # 测试报告
  test_reports/coverage/index.html # 覆盖率报告
  test_reports/junit.xml           # JUnit XML报告


🔢 测试统计
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  测试文件:   6个
  测试用例:   238个
  测试模块:   test_wifi_analyzer.py        (26个测试)
             test_wifi6_analyzer.py       (41个测试)
             test_heatmap.py              (28个测试)
             test_security_scoring.py     (66个测试)
             test_utils.py                (64个测试)
             test_config_manager.py       (25个测试)


⏱️ 运行时间
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  快速测试:   ~30秒
  完整测试:   ~2分钟
  CI测试:     ~3分钟


🏷️ 测试标记说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  @pytest.mark.integration         多模块协作测试
  @pytest.mark.performance         性能基准测试
  @pytest.mark.slow                运行时间 > 5秒
  @pytest.mark.admin_required      需要管理员权限


💡 常用场景
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  开发时测试:     python run_tests.py --quick
  提交前测试:     python run_tests.py
  CI/CD测试:      python run_tests.py --ci
  调试失败:       python run_tests.py --failed
  性能分析:       python run_tests.py --marker performance


🚀 pytest原生命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pytest -v                        # 详细输出
  pytest -s                        # 显示print
  pytest -x                        # 遇到失败立即停止
  pytest -k test_wifi6             # 运行名称匹配的测试
  pytest --lf                      # 仅运行上次失败的
  pytest --ff                      # 先运行上次失败的
  pytest -n auto                   # 并行运行（需pytest-xdist）
  pytest --durations=10            # 显示最慢的10个测试


🔧 环境准备
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pip install pytest pytest-cov pytest-html
  pip install -r requirements.txt


📚 相关文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  TESTING.md           # 完整测试文档
  .github/workflows/   # CI/CD配置
  pytest.ini           # pytest配置文件
  run_tests.py         # 测试运行器源码


╔════════════════════════════════════════════════════════════════════╗
║  提示: 首次运行建议使用 python run_tests.py --summary 查看概览     ║
╚════════════════════════════════════════════════════════════════════╝
